variables:
  ml-ws-connection: 'mm-aml-dev-pipelines' # Workspace Service Connection name
  ml-ws: 'mm-aml-dev' # AML Workspace name
  ml-rg: 'mm-machine-learning-dev-rg' # AML resource Group name
  ml-ct: 'cpu-cluster-3' # AML Compute cluster name
  ml-path: 'models/diabetes' # Model directory path in repo
  ml-exp: 'exp-test' # Experiment name
  ml-model-name: 'diabetes-model' # Model name
  ml-aks-name: 'aks-prod' # AKS cluster name
